"""
Configuration — Explainable Churn Prediction
All path constants used across the project are defined here.
"""
from pathlib import Path

# ── Directory Layout ───────────────────────────────────────────────────────────
# src/config.py lives in  <root>/src/  →  BASE_DIR is <root>/
BASE_DIR = Path(__file__).resolve().parent.parent

DATA_DIR      = BASE_DIR / "data"
MODEL_DIR     = DATA_DIR / "models"
PROCESSED_DIR = DATA_DIR / "processed"

# Create directories if they don't exist
for _dir in [DATA_DIR, MODEL_DIR, PROCESSED_DIR]:
    _dir.mkdir(parents=True, exist_ok=True)

# ── Input Data ─────────────────────────────────────────────────────────────────
DATASET_PATH  = DATA_DIR / "churn.csv"
RAW_DATA_PATH = DATASET_PATH       # alias used by preprocess.py

# ── Model Artefacts ────────────────────────────────────────────────────────────
# Standalone logistic-regression model (30 features, drop_first=True dummies)
MODEL_PATH    = DATA_DIR / "logistic_regression_model.pkl"

# Full sklearn Pipeline (preprocessor + RandomForest) — generated by train.py
PIPELINE_PATH = DATA_DIR / "churn_pipeline.pkl"

# Column names saved at train time so predict.py never reads the CSV again
MODEL_COLUMNS_PATH = DATA_DIR / "model_columns.pkl"

# StandardScaler (used by preprocess.py standalone workflow)
SCALER_PATH        = DATA_DIR / "scaler.pkl"

# Feature names saved by preprocess.py standalone workflow
FEATURE_NAMES_PATH = DATA_DIR / "feature_names.pkl"

# ── Processed Arrays (preprocess.py standalone workflow) ───────────────────────
X_TRAIN_PATH = PROCESSED_DIR / "X_train.npy"
X_TEST_PATH  = PROCESSED_DIR / "X_test.npy"
Y_TRAIN_PATH = PROCESSED_DIR / "y_train.npy"
Y_TEST_PATH  = PROCESSED_DIR / "y_test.npy"

# ── Visualisation Outputs ──────────────────────────────────────────────────────
FEATURE_IMPORTANCE_PATH        = DATA_DIR / "feature_importance.png"
FEATURE_IMPORTANCE_GLOBAL_PATH = DATA_DIR / "feature_importance_global.png"
SHAP_IMPORTANCE_PATH           = DATA_DIR / "shap_importance.png"

# ── Model Hyper-parameters ─────────────────────────────────────────────────────
RANDOM_STATE = 42
TEST_SIZE    = 0.2